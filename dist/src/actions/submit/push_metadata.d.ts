import { TContext } from '../../lib/context';
import { Branch } from '../../wrapper-classes/branch';
export declare function pushMetadata(branchesPushedToRemote: Branch[], context: TContext): Promise<void>;
