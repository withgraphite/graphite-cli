import { TContext } from '../lib/context';
export declare function restackBranches(branchNames: string[], context: TContext): void;
export declare function continueRestack(opts: {
    addAll: boolean;
}, context: TContext): void;
